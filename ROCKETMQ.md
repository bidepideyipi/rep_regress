MySQL Master/Slave

  Writer → Master (写) → Slave (只读复制)
           ↓
      Readers → Slave

  - 目的：读写分离，分担读压力
  - Slave：可以处理读请求

  RocketMQ 4.x Master/Slave

  Producer/Consumer → Master (读写)
                        ↓
                    Slave (数据备份，不提供服务)

  - 目的：数据冗余、高可用（非读写分离）
  - Slave：只备份数据，不对外提供服务
  - 客户端（Producer/Consumer）只连接 Master

  关键区别

  ┌──────────────────┬──────────────────────┬──────────────────┐
  │       特性       │        MySQL         │   RocketMQ 4.x   │
  ├──────────────────┼──────────────────────┼──────────────────┤
  │ Slave 服务客户端 │ ✅ 可以读            │ ❌ 不对外服务    │
  ├──────────────────┼──────────────────────┼──────────────────┤
  │ 主要目的         │ 读写分离、负载均衡   │ 数据备份、高可用 │
  ├──────────────────┼──────────────────────┼──────────────────┤
  │ 客户端连接       │ 可连 Master 或 Slave │ 只连 Master      │
  └──────────────────┴──────────────────────┴──────────────────┘

  5.0 的变化

  RocketMQ 5.0 引入 Proxy 后，Proxy 可以从 Slave 拉取历史消息（部分场景），但架构理念还是备份优先于读写分离。

  RocketMQ 5.0 vs 4.x 主要升级

  🏗️ 架构层面

  ┌────────────┬──────┬───────────────────────────────────────┐
  │    特性    │ 4.x  │                  5.0                  │
  ├────────────┼──────┼───────────────────────────────────────┤
  │ 存算分离   │ ❌   │ ✅ 存储和计算独立扩展                 │
  ├────────────┼──────┼───────────────────────────────────────┤
  │ Controller │ ❌   │ ✅ 新增控制器模块                     │
  ├────────────┼──────┼───────────────────────────────────────┤
  │ Proxy      │ ❌   │ ✅ 新增代理组件                       │
  ├────────────┼──────┼───────────────────────────────────────┤
  │ 主从切换   │ 手动 │ ✅ 自动主从切换 + Kafka风格Master选举 │
  └────────────┴──────┴───────────────────────────────────────┘

  🔧 API & SDK

  ┌──────────┬────────────────┬────────────────────────────────────────┐
  │   特性   │      4.x       │                  5.0                   │
  ├──────────┼────────────────┼────────────────────────────────────────┤
  │ SDK 设计 │ 重量级富客户端 │ 轻量级 API                             │
  ├──────────┼────────────────┼────────────────────────────────────────┤
  │ 协议     │ Remoting       │ ✅ 新增 gRPC                           │
  ├──────────┼────────────────┼────────────────────────────────────────┤
  │ 兼容性   │ -              │ 兼容 4.x & 5.x SDK（gRPC 仅支持 5.0+） │
  └──────────┴────────────────┴────────────────────────────────────────┘

  RocketMQ 4.x 主从机制

  4.x 的主从模式

  4.x: Master-Slave 架构

  Broker-a (Master) ←→ Broker-a (Slave)
    brokerId=0         brokerId=1

  配置示例：
  # Master
  brokerId=0
  brokerRole=SYNC_MASTER

  # Slave
  brokerId=1
  brokerRole=SLAVE

  手工切换的意思

  4.5 版本之前：如果 Master 宕机，Slave 不会自动成为 Master，需要：

  # 1. 停止 Slave
  # 2. 修改配置 brokerId=0, brokerRole=SYNC_MASTER
  # 3. 重启作为新 Master