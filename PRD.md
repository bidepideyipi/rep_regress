# SLOT游戏平台 PRD文档

## 1. 产品概述

### 1.1 产品背景
本产品是一个高性能、高并发的SLOT游戏平台，支持浏览器端、移动端游戏体验；一个大厅集成多款子游戏；提供统一的认证网关服务，实现实时的RTP（Return to Player）计算和监控。 产品仅用于学习和研究，不用于商业用途。

### 1.2 核心目标
- 支持10000并发用户的实时游戏体验
- 实现全局、终端、玩家三个维度的RTP实时计算，延迟小于10秒（基于Flink流处理）
- 提供统一的认证网关，支持浏览器端访问
- 灵活的游戏配置系统，支持不同游戏规则配置
- 高可用性和可扩展性的系统架构

### 1.3 目标用户
- **集成商**：游戏平台运营商或第三方集成商，通过API获取游戏访问权限和地址
- **最终用户**：通过集成商平台访问游戏，体验SLOT游戏
- **系统管理员**：通过运营后台监控RTP数据和系统运行状态

## 2. 系统架构设计

### 2.1 整体架构图
```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              接入层                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │   浏览器端    │  │   移动端      │  │   集成商系统  │  │  管理后台   │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼──────────────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │                  │
          └──────────────────┼──────────────────┴──────────────────┘
                             ▼ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────────────────────┐
│                         认证网关服务                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  负载均衡层   │  │  API认证层   │  │  请求路由层   │  │  用户认证层   │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
└────────────────────────────┬───────────────────────────────────────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          ▼                  ▼                  ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   游戏服务       │ │  RTP计算服务    │ │  后台管理系统   │
│  ┌───────────┐ │ │  ┌───────────┐ │ │  ┌───────────┐  │
│  │ Nacos配置 │ │ │  │ 全局RTP   │ │ │  │ 用户管理   │  │
│  │ Godot引擎 │ │ │  │ 终端RTP   │ │ │  │ 日志管理   │  │
│  │ 游戏逻辑   │ │ │  │ 玩家RTP   │ │ │  │ RTP监控   │  │
│  └───────────┘ │ │  └───────────┘ │ │  └───────────┘  │
└─────────────────┘ └─────────────────┘ └─────────────────┘
          │                  │
          └──────────────────┼──────────────────┐
                             ▼                  ▼
┌───────────────────────────────────────────────────┐   ┌───────────────┐
│              消息队列与注册中心                    │   │  配置管理服务  │
│  ┌──────────────┐  ┌──────────────┐              │   │  ┌─────────┐ │
│  │  RocketMQ    │  │    Nacos     │              │   │  │Nacos配置│ │
│  │  消息队列    │  │  服务发现    │              │   │  └─────────┘ │
│  └──────────────┘  └──────────────┘              │   └───────────────┘
└───────────────────────────────────────────────────┘
                             │
                             ▼
┌───────────────────────────────────────────────────┐
│                    数据存储层                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │   Redis      │  │   MySQL      │  │ClickHouse│ │
│  │   缓存       │  │  业务数据    │  │ 游戏日志  │ │
│  └──────────────┘  └──────────────┘  └──────────┘ │
└───────────────────────────────────────────────────┘
```

### 2.2 数据处理架构：流批一体

#### 2.2.1 架构设计理念

本项目采用基于 Flink + ClickHouse 的**流批一体架构**，属于现代 Kappa 架构的演进版本：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           数据处理层（流批一体）                               │
│                                                                              │
│  游戏服务 → RocketMQ → Flink Stream → ClickHouse (明细数据 + 统计结果)       │
│                      ↑                                                        │
│                      │                                                        │
│                   历史数据重放（批处理模式：数据修正、重算）                    │
│                                                                              │
│  Flink 职责：实时流计算 + 批处理重算（单一代码路径）                          │
│  ClickHouse 职责：明细数据存储 + 计算结果存储 + 交互式查询                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.2.2 架构优势

| 优势 | 说明 |
|------|------|
| **代码复用** | 流处理和批处理使用同一套 Flink SQL/Job，避免维护双套代码 |
| **实时性** | Flink 流处理可实现秒级 RTP 计算，优于传统批处理（分钟级） |
| **数据修正** | 通过重放 RocketMQ 历史消息，可随时重算任意时间段的数据 |
| **存储分离** | ClickHouse 专注列式存储和查询，计算逻辑与存储解耦 |
| **查询灵活** | 保留明细数据，支持管理后台、数据分析的任意维度查询 |

#### 2.2.3 数据流设计

**实时流（Speed Layer）：**
```
游戏结算日志 → RocketMQ → Flink实时消费
                              ↓
                    窗口聚合（1分钟/5分钟滚动窗口）
                              ↓
                    RTP统计（全局/终端/玩家）
                              ↓
                    ClickHouse rtp_statistics_local
```

**批处理（Batch Layer，按需触发）：**
```
数据异常/需要修正 → 指定时间段RocketMQ消息重放
                              ↓
                    Flink批处理模式（同一套代码）
                              ↓
                    覆盖ClickHouse对应分区数据
```

#### 2.2.4 与传统架构对比

| 特征 | 传统Lambda架构 | 传统Kappa架构 | 本项目架构 |
|------|---------------|---------------|-----------|
| 代码路径 | 双套（批+流） | 单套 | 单套 |
| 批处理引擎 | Hadoop/Spark | 流处理引擎重放 | Flink批处理 |
| 主数据存储 | HDFS | Kafka日志 | ClickHouse |
| 历史数据修正 | 批处理重跑 | 消息重放 | 消息重放 |
| 查询合并 | 需要合并层 | 无需合并 | 无需合并 |

### 2.3 模块划分

#### 2.3.1 认证网关服务
- **功能职责**：统一接入认证、请求路由、负载均衡、用户注册、用户登陆
- **主要组件**：负载均衡层、API认证层、请求路由层、Golang gin服务

#### 2.3.2 游戏服务
- **功能职责**：游戏配置加载、游戏逻辑处理、RTP数据查询、游戏结果生成、结算日志记录异步投递
- **主要组件**：Nacos配置中心、Godot游戏引擎、Golang gin服务、RocketMQ消息队列、ClickHouse数据库、Redis缓存、MySQL数据库

#### 2.3.3 RTP计算服务
- **功能职责**：基于Flink流批一体架构，实时消费结算日志并计算RTP数据（全局、终端、玩家三个维度），计算结果写入ClickHouse数据库
- **主要组件**：Nacos配置中心、RocketMQ消息队列、Apache Flink流处理引擎、ClickHouse数据库、MySQL数据库

#### 2.3.4 配置管理服务
- **功能职责**：游戏配置管理、中间件配置管理
- **主要组件**：Nacos配置中心

#### 2.3.5 后台管理系统
- **功能职责**：用户管理、日志管理、RTP数据监控
- **主要组件**：GoAdmin框架、MySQL数据库

## 3. 功能需求

### 3.1 认证网关功能

#### 3.1.1 集成商接入管理
- 集成商由运营后台统一管理，生成独立的API Key和密钥
- 基于API Key和密钥的签名认证
- 集成商根据游戏ID、用户ID等参数获取加密游戏地址
- 支持Token生成、刷新和过期管理
- 基于集成商配置控制游戏访问权限

#### 3.1.2 用户注册与登录
- 支持手机号/邮箱注册
- 支持多种登录方式（账号密码、短信验证码、第三方登录）
- 用户信息管理与密码找回

#### 3.1.3 请求路由
- 解析加密游戏地址，路由到对应的Godot游戏实例
- 基于权重和连接数的负载均衡
- 动态服务注册和发现
- 自动故障检测和转移

### 3.2 游戏服务功能

#### 3.2.1 游戏配置管理
- 游戏配置由运营后台统一管理，包括符号定义、卷轴权重配置、赔付表配置
- 游戏服务启动时加载最新配置，支持配置热更新
- 支持多款游戏，每款游戏有自己的配置和逻辑

#### 3.2.2 游戏核心流程
- **游戏开始**：初始化游戏会话，生成唯一游戏ID
- **旋转结算**：根据配置规则生成结果并计算赔付
- **特殊功能**：触发Free Spins、Bonus Games等特殊功能
- **游戏结束**：结算并记录游戏结果

#### 3.2.3 结算计算
- 根据赔付表匹配有效符号组合
- 基于下注金额和赔付倍数计算赔付金额
- 处理Wild符号、Multipliers等多倍数规则
- 处理特殊功能的额外赔付

#### 3.2.4 结算日志异步投递
- 游戏结算完成后，将结算日志异步投递到消息队列
- 保证日志投递的可靠性和顺序性

### 3.3 RTP计算服务功能

#### 3.3.1 全局RTP计算
- **计算维度**：所有终端、所有玩家的总体RTP
- **计算周期**：实时计算，延迟<60秒
- **计算公式**：`全局RTP = (总赔付金额 / 总下注金额) × 100%`

#### 3.3.2 终端RTP计算
- **计算维度**：单个终端的所有游戏数据
- **计算周期**：实时计算，延迟<60秒
- **计算公式**：`终端RTP = (终端赔付金额 / 终端下注金额) × 100%`

#### 3.3.3 玩家RTP计算
- **计算维度**：单个玩家的游戏数据
- **计算周期**：实时计算，延迟<60秒
- **计算公式**：`玩家RTP = (玩家赔付金额 / 玩家下注金额) × 100%`

#### 3.3.4 RTP数据查询与告警
- 提供RTP数据的实时查询接口
- 支持指定时间范围的历史RTP数据
- 支持按不同维度查询RTP数据
- RTP异常时自动告警

### 3.4 后台管理系统功能

#### 3.4.1 用户管理
- 用户信息查询与管理
- 用户权限配置
- 用户行为日志查看

#### 3.4.2 日志管理
- 游戏日志查询
- 操作日志审计
- 异常日志告警

#### 3.4.3 RTP数据监控
- 实时展示全局、终端、玩家维度的RTP数据
- 支持自定义时间范围查询
- RTP异常告警展示

## 4. 非功能性需求

### 4.1 性能要求

#### 4.1.1 并发性能
- **并发用户数**：支持10000并发用户
- **峰值TPS**：支持5000TPS
- **响应时间**：
  - 游戏结算请求：<150ms
  - RTP计算查询：<25ms
  - 认证请求：<25ms

#### 4.1.2 RTP计算延迟
- **全局RTP**：<10秒（基于Flink流处理，1分钟滚动窗口）
- **终端RTP**：<10秒
- **玩家RTP**：<30秒
- **数据刷新频率**：1分钟级实时计算

### 4.2 可用性要求
- **系统可用性**：99.5%
- **故障恢复时间**：<5分钟
- **数据一致性**：最终一致性要求
- **备份策略**：实时热备 + 每日冷备

### 4.3 扩展性要求
- 支持服务实例水平扩展
- 支持配置升级扩展
- 支持游戏配置的实时更新
- 支持动态添加新游戏实例

### 4.4 安全性要求
- 传输层TLS加密，敏感数据加密存储
- 基于角色的权限控制
- 完整的操作审计日志
- 防SQL注入、XSS、CSRF等攻击

### 4.5 可维护性要求
- 结构化日志，便于问题排查
- 完善的监控和告警系统
- 支持自动化部署和回滚
- 完整的技术文档和API文档

## 5. 技术实现方案

### 5.1 后端技术栈
- **编程语言**：Go 1.21
- **流处理引擎**：Apache Flink 2.2.1（流批一体架构）
- **数据库**：MySQL 8.0
- **统计分析**：ClickHouse 24.0
- **缓存**：Redis 7.0
- **消息队列**：RocketMQ 4.9.8
- **服务注册发现**：Nacos 2.0
- **配置中心**：Nacos 2.0

### 5.2 前端技术栈
- **游戏客户端**：Godot Engine（浏览器端Web应用）
- **管理后台**：Vue3 + Element Plus / GoAdmin

**Godot技术特点**：
- 支持导出为HTML5 Web应用
- 内置HTTP请求功能，支持异步调用
- 内置JSON序列化/反序列化
- 引擎体积小，加载快速
- GDScript类Python脚本语言，开发效率高

### 5.3 通信方案
Godot游戏Web应用与后端通过HTTP/HTTPS REST API通信，采用RESTful API设计，基于JSON格式，HTTPS加密传输。

### 5.4 流批一体架构实现细节

#### 5.4.1 Flink Job 设计

| Job 类型 | 输入源 | 处理逻辑 | 输出目标 |
|---------|-------|---------|---------|
| 明细数据写入 | RocketMQ 游戏日志 | 解析、清洗、格式转换 | ClickHouse game_log_detail_local |
| 实时 RTP 计算 | RocketMQ 游戏日志 | 1/5分钟滚动窗口聚合 | ClickHouse rtp_statistics_local |
| 历史数据修正 | RocketMQ 历史消息 | 批处理模式重算 | ClickHouse 分区覆盖 |

#### 5.4.2 窗口策略

| 统计维度 | 窗口类型 | 窗口大小 | 滑动步长 | 输出频率 |
|---------|---------|---------|---------|---------|
| 全局 RTP | 滚动窗口 | 1分钟 | 1分钟 | 实时 |
| 终端 RTP | 滚动窗口 | 1分钟 | 1分钟 | 实时 |
| 玩家 RTP | 滚动窗口 | 5分钟 | 1分钟 | 实时 |

#### 5.4.3 RocketMQ 消息保留策略

为支持历史数据重放，RocketMQ Topic 配置：
- 消息保留时间：7天（支持近一周内数据修正）
- 消息最大大小：4MB
- 同步刷盘：保证数据不丢失

---

**最后更新**：2025-06-07（更新：引入 Flink 流批一体架构）